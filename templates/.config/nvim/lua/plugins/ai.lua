return {
  "Exafunction/codeium.nvim",
  dependencies = {
    "nvim-lua/plenary.nvim",
    "hrsh7th/nvim-cmp",
  },
  config = function()
    require("codeium").setup({})
  end,
  -- Make sure this loads after nvim-cmp is configured
  after = { "nvim-cmp" }
}
